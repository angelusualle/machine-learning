
### How to run:
Using python3.11:
0. Open a terminal to this `lda` directory.
1. `python -m venv .venv` to create a virtual environment.
2. `source .venv/bin/activate` to activate the environment.
2. `pip install -r requirements.txt` to install requirements.